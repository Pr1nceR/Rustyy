# .understandignore
# Syntax: same as .gitignore
# Built-in defaults (always excluded): node_modules/, .git/, dist/, build/, *.lock, *.min.js, etc.
#
# --- From .gitignore (uncomment to exclude) ---

# .env
# instances
# credentials
# maps
# logs
# temp

# --- Detected directories (uncomment to exclude) ---

# docs/

# --- Test file patterns (uncomment to exclude) ---

# *.test.*
# *.spec.*
# *.snap
